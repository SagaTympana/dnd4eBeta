/**
  * @typedef SubtypeMetadata
  * @property {string} type							The registered document subtype in system.json.
  * @property {string} icon							A FontAwesome icon that can be added to `typeIcons`, e.g. `"fa-solid fa-user"`.
  * @property {Record<string, string>} embedded		Record of document names of pseudo-documents and the path to the collection.
  */
