/**
 * @typedef PseudoDocumentMetadata
 * @property {string} documentName					The document name of this pseudo-document.
 * @property {string|null} defaultImage				File path for a default image.
 * @property {string} icon							The font-awesome icon for this pseudo-document type.
 * @property {Record<string, string>} embedded		Record of document names of pseudo-documents and the path to the collection.
 * @property {sheetClass} PseudoDocumentSheet		The class used to render this pseudo-document.
 */
